# 运行模式
GIN_MODE=release

# 应用配置
APP_NAME=WebStack
APP_HOST=0.0.0.0
APP_PORT=8080

###########################################
# 数据库选择 - 二选一
# 1. 使用SQLite（默认，简单，无需额外配置）
# 2. 使用外部MySQL服务器（需设置连接参数）
###########################################

# 选项1: SQLite配置（默认）
DB_TYPE=sqlite
SQLITE_FILE=/app/db/webstack.db

# 选项2: MySQL连接配置
# 如需使用外部MySQL，取消下面的注释并设置正确的连接参数
# DB_TYPE=mysql
# MYSQL_HOST=your-mysql-server  # 外部MySQL服务器地址
# MYSQL_PORT=3306
# MYSQL_USER=your_mysql_user    # 连接MySQL的用户名
# MYSQL_PASSWORD=your_password  # 连接MySQL的密码
# MYSQL_DATABASE=webstack       # 数据库名
# MYSQL_CHARSET=utf8mb4
# MYSQL_LOC=Local